# План реализации CRM «Хорошие Бассейны»

> Прогресс трекается галочками. Когда юзер начинает новый диалог — Claude читает этот файл и понимает где остановились.
> Каждый этап заканчивается **чекпойнтом** — конкретным действием, которое юзер выполняет вручную для подтверждения.

**Легенда:**
- `[ ]` — не сделано
- `[~]` — в процессе
- `[x]` — сделано и проверено юзером

---

## Этап 1. Setup проекта

- [x] Инициализировать Next.js 16 + TypeScript + App Router (Next 16.2.6, React 19.2.4, Turbopack)
- [x] Установить Tailwind CSS (v4 через @tailwindcss/postcss)
- [~] Установить shadcn/ui — **отложено в этап 2** (новая версия требует интерактивный выбор пресета, поставим вместе с первой реальной формой)
- [x] Установить Prisma, инициализировать (Prisma 7.8 + dotenv, конфиг в `prisma.config.ts`)
- [x] Создать БД `good_pools_crm_db` в локальном Postgres
- [x] Настроить `.env` (DATABASE_URL, NEXTAUTH_SECRET, VAPID-плейсхолдеры, SMTP, Я.Карты)
- [x] Базовая структура папок: `src/app/`, `src/lib/`, `src/components/`, `prisma/`
- [x] Layout заготовка с шапкой «ХОРОШИЕ БАССЕЙНЫ» (`src/components/Header.tsx`)
- [x] Заглушка страницы `/login` (форма email + пароль, кнопка disabled до этапа 2)
- [x] `npm run dev` поднимает приложение на http://localhost:3000 (проверено `curl`: / → 200, /login → 200)

**Чекпойнт:** Юзер открывает http://localhost:3000 → видит шапку с логотипом и кнопку «Войти». Кнопка ведёт на `/login` с формой.

---

## Этап 2. Auth + роли

- [ ] **Установить shadcn/ui** (перенесено из этапа 1) — `npx shadcn@latest init`, выбрать preset «Nova» интерактивно
- [ ] Prisma-схема: `User` (id, email, password_hash, role, name, phone, created_at)
- [ ] Миграция применена
- [ ] Установить `next-auth` + `bcrypt`
- [ ] Credentials provider с проверкой email/пароля и bcrypt
- [ ] Сессии хранятся в БД (`Session`, `Account` таблицы)
- [ ] Middleware проверяет авторизацию и редиректит неавторизованных на `/login`
- [ ] Логика ролей: `admin | service | client` с роут-разделением (`/admin/*`, `/service/*`, `/client/*`)
- [ ] Скрипт `scripts/create-admin.ts` — создаёт первого админа из консоли
- [ ] Заглушки главных страниц для каждой роли (просто «Привет, админ» / «Привет, сервисник» / «Привет, клиент»)

**Чекпойнт:** Юзер запускает `npx tsx scripts/create-admin.ts admin@example.com pass123 "Иван Админ"` → логинится через `/login` → попадает на `/admin` и видит «Привет, Иван Админ (admin)».

---

## Этап 3. Управление пользователями

- [ ] Раздел `/admin/users` — таблица всех пользователей (фильтр по роли)
- [ ] Кнопка «Добавить сервисника» — форма (имя, email, телефон) → создаётся `User` с ролью `service`, отправляется email-приглашение
- [ ] Кнопка «Добавить клиента» — форма (ФИО, email, телефон, юр.инфо?) → создаётся `User` (role=client) + `Customer`, отправляется email-приглашение
- [ ] Email-инвайт содержит одноразовую ссылку на установку пароля (`/setup-password?token=...`)
- [ ] Страница `/setup-password` — форма пароля + подтверждение, после установки → автологин
- [ ] Настройка SMTP в `.env` (для разработки можно использовать Mailpit/Mailhog в docker)
- [ ] Возможность деактивировать пользователя (мягкое удаление: флаг `active=false`)
- [ ] Логирование действий в `ActivityLog` (создание/деактивация пользователя)

**Чекпойнт:** Админ заводит сервисника и клиента → проверяет почтовый ящик (Mailpit на localhost:8025) → переходит по ссылке → ставит пароль → заходит как сервисник/клиент.

---

## Этап 4. Карточка клиента + бассейны

- [ ] Prisma: `Customer`, `Pool` (адрес, lat, lng, photos[], facing_materials, extra_field, instructions[])
- [ ] Раздел `/service/customers` (и `/admin/customers`) — список клиентов
- [ ] Карточка клиента: контакты + список его бассейнов
- [ ] Создание/редактирование бассейна
- [ ] Загрузка фото объекта (мягкий лимит 10, drag-drop, превью, удаление)
- [ ] Адрес + Яндекс.Карта (выбор точки кликом, автозаполнение через геокодер)
- [ ] Кнопка «Построить маршрут» открывает Я.Карты в новой вкладке
- [ ] Поле «Облицовочные покрытия» (текст / список)
- [ ] Поле «Дополнительная графа» (свободный текст)
- [ ] Раздел «Инструкции» — загрузка PDF/текст/ссылки
- [ ] Поле «Индивидуальная цена сервиса» (без истории)

**Чекпойнт:** Сервисник создаёт клиента «Тест Тестов» с двумя бассейнами по разным адресам → загружает фото → проверяет точку на карте → жмёт «Построить маршрут» → открывается Я.Карты.

---

## Этап 5. Регламент оборудования

- [ ] Prisma: `EquipmentTemplate` (тип, гарантия по умолчанию, период регламента) + `Equipment` (привязка к Pool)
- [ ] Раздел `/admin/equipment-templates` — CRUD шаблонов оборудования
- [ ] В карточке бассейна — раздел «Оборудование»: добавить из шаблона, указать дату установки, серийник
- [ ] Расчёт даты окончания гарантии = install_date + warranty_months
- [ ] Расчёт даты следующего регламента = last_replacement_date + regulation_period_days
- [ ] В карточке оборудования — кнопка «Заменено сегодня» → обновляет last_replacement_date
- [ ] Виджет на главной сервисника: «Скоро регламент» (за 7 дней) и «Скоро гарантия» (за 14)

**Чекпойнт:** Админ создаёт шаблон «Датчик хлора, гарантия 6 мес, регламент 90 дней» → сервисник добавляет такой датчик в бассейн → видит в виджете дату окончания гарантии и регламента.

---

## Этап 6. Чек-лист (динамическая схема)

- [ ] Prisma: `ChecklistQuestion` (порядок, тип, label, опции, флаг active)
- [ ] Импорт вопросов из существующей Google-формы (один раз вручную: посмотреть форму, создать сидер)
- [ ] Раздел `/admin/checklist` — список вопросов, drag-drop сортировка, добавление/редактирование/деактивация
- [ ] Поддерживаемые типы: текст, число, селект, чекбокс/да-нет
- [ ] Превью «как видит сервисник» в админке

**Чекпойнт:** Админ открывает `/admin/checklist` → видит вопросы из Google-формы → меняет порядок → добавляет новый вопрос «Уровень pH (число)» → видит в превью.

---

## Этап 7. Календарь и онлайн-запись

- [ ] Prisma: `Visit`, `OnlineRequest`, `VisitSeries`
- [ ] Раздел `/service/calendar` — недельный/месячный вид всех визитов всех сервисников
- [ ] Создание визита вручную (выбор клиента, бассейна, даты, времени, длительности, исполнителя)
- [ ] Создание серии визитов (повторение раз в неделю / две недели / месяц на N повторений)
- [ ] Раздел `/client/request-visit` — клиент выбирает бассейн, указывает желаемый диапазон дат, текст-комментарий
- [ ] Если у клиента есть долг → форма заблокирована с сообщением «Оплатите предыдущий визит»
- [ ] Раздел `/service/online-requests` — список заявок, кнопка «Принять» → форма с диапазоном дат от сервисника → создаётся `Visit`, заявка помечается `accepted`
- [ ] Push-уведомления: новая заявка → всем сервисникам; принята → клиенту (заглушка, реальный push в этапе 12)

**Чекпойнт:** Клиент через ЛК отправляет заявку → сервисник видит её в `/service/online-requests` → принимает с диапазоном «12-15 мая» → видит созданный визит в календаре → клиент видит в ЛК статус «принято».

---

## Этап 8. Визит, отчёт, PDF

- [ ] Раздел `/service/visit/[id]` — мобильно-адаптивная страница визита
- [ ] Динамический рендер чек-листа из `ChecklistQuestion`
- [ ] Прикрепление фото визита (1+ фото, с компрессией через sharp)
- [ ] Добавление доп.работ (название + сумма, можно несколько)
- [ ] Добавление химии из прайса (выбор + количество, цена фиксируется на момент)
- [ ] Кнопка «Завершить визит» → статус `completed`, генерация PDF
- [ ] PDF: логотип «ХОРОШИЕ БАССЕЙНЫ», ФИО сервисника, дата, ответы чек-листа, фото, таблица доп.работ + химии, итоговая сумма
- [ ] PDF сохраняется в `uploads/reports-pdf/{visit_id}/report.pdf`
- [ ] Клиент видит отчёт в `/client/visits/[id]` (PDF + html-копия)
- [ ] Возможность редактирования отчёта сервисником после завершения (логируется в ActivityLog)

**Чекпойнт:** Сервисник с телефона открывает запланированный визит → заполняет чек-лист → прикрепляет фото → добавляет «Замена картриджа — 1500 ₽» и «Хлор 5 кг» → завершает → клиент в своём ЛК видит PDF.

---

## Этап 9. Прайс химии (админ)

- [ ] Prisma: `ChemistryItem` (имя, единица, цена, active)
- [ ] Раздел `/admin/chemistry` — таблица CRUD
- [ ] Деактивированные позиции не показываются в визите, но остаются в истории
- [ ] Логирование изменения цен в `ActivityLog`

**Чекпойнт:** Админ добавляет «Хлор гранулированный, кг, 800 ₽» → сервисник видит позицию при добавлении химии в визит.

---

## Этап 10. Реестры и журнал

- [ ] Раздел `/admin/registry/checklists` — таблица всех ответов всех визитов: визит / клиент / бассейн / дата / колонки = вопросы
- [ ] Кнопка «Экспорт в Excel» (через `exceljs`) и «Экспорт в CSV»
- [ ] Раздел `/admin/registry/visits` — таблица всех визитов с фильтрами (период, клиент, сервисник)
- [ ] Раздел `/admin/activity-log` — журнал действий с фильтрами (по пользователю, по типу события, по дате)
- [ ] Раздел `/admin/registry/customers` — таблица всех клиентов с долгами/балансом

**Чекпойнт:** Админ открывает реестр чек-листов → видит таблицу ответов → нажимает «Экспорт в Excel» → файл скачивается с корректными данными.

---

## Этап 11. Чат поддержки

- [ ] Prisma: `ChatThread`, `ChatMessage`
- [ ] Раздел `/client/support` — один тред с сообщениями, форма ввода + прикрепление фото
- [ ] Раздел `/service/support` (и `/admin/support`) — список всех тредов, открытие отдельного треда
- [ ] Поллинг каждые 5 секунд при открытом треде (новые сообщения подтягиваются)
- [ ] Photo-вложения сохраняются в `uploads/chat/{thread_id}/`
- [ ] Push-уведомление о новом сообщении (заглушка, реальный push в этапе 12)
- [ ] Индикатор «прочитано»

**Чекпойнт:** Клиент пишет в поддержку «Не работает фильтр» → сервисник видит сообщение через 5 сек → отвечает → клиент видит ответ через 5 сек.

---

## Этап 12. Web Push уведомления

- [ ] Сгенерировать VAPID-ключи, положить в `.env`
- [ ] Service Worker (`public/sw.js`) обрабатывает push-events
- [ ] Подписка пользователя при первом входе (запрос permission)
- [ ] Сохранение подписки в `PushSubscription`
- [ ] Серверная функция `sendPush(userId, title, body, payload)`
- [ ] Подключить ко всем событиям из этапа 7 и 11
- [ ] Уведомление об окончании гарантии (за 14 дней) — пока заглушка-функция, реальный cron в этапе 15
- [ ] Уведомление о регламенте (за 7 дней) — то же

**Чекпойнт:** Юзер ставит CRM на главный экран iPhone (или Android) → разрешает уведомления → тестовая кнопка `/admin/test-push` отправляет себе пуш → пуш приходит даже когда браузер закрыт.

---

## Этап 13. Оплата (полу-онлайн)

- [ ] Prisma: `PaymentStatus` на `Visit` (paid | unpaid | online_pending) + поле `paid_at`
- [ ] В карточке визита сервисник/админ может отметить «Оплачено наличкой/переводом» с датой
- [ ] Расчёт «долга» клиента = сумма всех `unpaid` визитов
- [ ] При попытке клиента создать онлайн-заявку с долгом > 0 → блок с сообщением
- [ ] В админке `/admin/registry/customers` — колонка «Долг»
- [ ] Заготовка `lib/payments/provider.ts` с интерфейсом `PaymentProvider` (метод `createPayment(visit_id, amount)`) + dummy-имплементация. Реальный провайдер подключим позже одной правкой.

**Чекпойнт:** Сервисник отмечает визит как «Оплачено» → у клиента долг 0 → клиент может создать новую онлайн-заявку. Если визит не оплачен → форма заявки заблокирована.

---

## Этап 14. PWA

- [ ] Установить Serwist
- [ ] `manifest.webmanifest` с иконками и названием «Хорошие Бассейны CRM»
- [ ] Иконки 192x192 и 512x512 (логотип компании)
- [ ] Service worker регистрируется автоматически
- [ ] Offline-fallback страница
- [ ] iOS: meta-теги для «Add to Home Screen»
- [ ] Splash-screen для iOS

**Чекпойнт:** Юзер открывает CRM на iPhone в Safari → «Поделиться» → «На экран Домой» → иконка появляется → запуск открывает приложение в полноэкранном режиме без браузерной шапки.

---

## Этап 15. Cron-задачи

- [ ] Worker-процесс `worker/index.ts` запускается отдельно (`npm run worker`)
- [ ] Cron «ежедневно в 09:00»: проверить оборудование с гарантией, истекающей через 14 дней → push клиенту
- [ ] Cron «ежедневно в 09:00»: проверить регламент за 7 дней → push клиенту + всем сервисникам
- [ ] Cron «ежедневно в 03:00»: удалить файлы из `uploads/visit-photos/` и `uploads/chat/` старше 90 дней (физически с диска + записи в БД)
- [ ] Cron «каждые 5 минут»: rescheduled — отправить отложенные пуши из очереди (если делаем очередь)
- [ ] Логирование всех cron-прогонов

**Чекпойнт:** Запускаем `npm run worker` → ставим в шаблоне регламент 8 дней назад → ждём 1 минуту (или меняем расписание на «каждую минуту») → видим лог «Отправлен пуш о регламенте клиенту X» и сам пуш приходит.

---

## Этап 16. Деплой на боевой сервер

- [ ] Dockerfile для Next.js (multi-stage, standalone output)
- [ ] Dockerfile для worker
- [ ] `docker-compose.yml`: postgres + app + worker + nginx
- [ ] `nginx.conf` с reverse-proxy на app, отдача `/uploads/` через защищённый роут (не напрямую)
- [ ] LetsEncrypt через `certbot` или `nginx-certbot` контейнер
- [ ] `.env.production` шаблон
- [ ] Скрипт деплоя `deploy.sh` (git pull + docker compose up -d --build)
- [ ] Скрипт бэкапа `backup-db.sh` + cron на сервере (ежедневно в 02:00, ротация 30 дней)
- [ ] Документация `DEPLOY.md` с инструкциями для системника клиента

**Чекпойнт:** Открываем боевой URL компании в браузере → видим логин-страницу с HTTPS-сертификатом → логинимся как админ → всё работает как локально.

---

## Этап 17. Финальная приёмка

- [ ] Прогон сценария «Админ заводит сервисника и 3 клиентов с бассейнами»
- [ ] Прогон сценария «Клиент отправляет заявку → сервисник принимает → визит → PDF»
- [ ] Прогон сценария «Клиент пишет в чат → ответ сервисника → пуш приходит»
- [ ] Прогон сценария «Регламент через 7 дней → пуши приходят»
- [ ] Прогон сценария «Долг → блокировка записи»
- [ ] Прогон сценария «Экспорт реестра → файл корректный»
- [ ] Чек-лист безопасности: проверка прав доступа к чужим файлам, к чужим визитам, SQL-инъекции (Prisma защищает), XSS в комментариях
- [ ] Передача доступов клиенту, обучение

**Чекпойнт:** Клиент использует CRM в реальной работе минимум неделю без блокирующих багов.

---

## Как продолжить работу в новой сессии

1. Открой `~/good-pools-crm/plan.md` (этот файл).
2. Найди первый незавершённый этап (`[ ]`).
3. Скажи Claude: «Продолжаем CRM с этапа N».
4. Claude прочитает `spec.md` + `plan.md` и поймёт контекст.

**Текущий статус:** этап 1 готов (ждёт чекпойнта юзера). Этапы 2-17 — pending.
