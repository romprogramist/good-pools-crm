# План реализации CRM «Хорошие Бассейны»

> Прогресс трекается галочками. Когда юзер начинает новый диалог — Claude читает этот файл и понимает где остановились.
> Каждый этап заканчивается **чекпойнтом** — конкретным действием, которое юзер выполняет вручную для подтверждения.

**Легенда:**
- `[ ]` — не сделано
- `[~]` — в процессе
- `[x]` — сделано и проверено юзером

---

## Этап 1. Setup проекта

- [x] Инициализировать Next.js 16 + TypeScript + App Router (Next 16.2.6, React 19.2.4, Turbopack)
- [x] Установить Tailwind CSS (v4 через @tailwindcss/postcss)
- [x] Установить shadcn/ui — **отложено в этап 2** (новая версия требует интерактивный выбор пресета, поставим вместе с первой реальной формой)
- [x] Установить Prisma, инициализировать (Prisma 7.8 + dotenv, конфиг в `prisma.config.ts`)
- [x] Создать БД `good_pools_crm_db` в локальном Postgres
- [x] Настроить `.env` (DATABASE_URL, NEXTAUTH_SECRET, VAPID-плейсхолдеры, SMTP, Я.Карты)
- [x] Базовая структура папок: `src/app/`, `src/lib/`, `src/components/`, `prisma/`
- [x] Layout заготовка с шапкой «ХОРОШИЕ БАССЕЙНЫ» (`src/components/Header.tsx`)
- [x] Заглушка страницы `/login` (форма email + пароль, кнопка disabled до этапа 2)
- [x] `npm run dev` поднимает приложение на http://localhost:3000 (проверено `curl`: / → 200, /login → 200)

**Чекпойнт:** Юзер открывает http://localhost:3000 → видит шапку с логотипом и кнопку «Войти». Кнопка ведёт на `/login` с формой.

---

## Этап 2. Auth + роли

- [x] **shadcn/ui** установлен (preset Nova) + компоненты button/input/label/card
- [x] Prisma-схема: `User` (cuid, email, name, phone, passwordHash, role, active, timestamps) + `Account`/`Session`/`VerificationToken` для Auth.js
- [x] Миграция `20260507231222_init_auth` применена
- [x] Prisma переведён на стабильный `prisma-client-js` + адаптер `@prisma/adapter-pg`
- [x] `next-auth@beta` (v5) + Credentials provider + JWT-сессия + bcryptjs
- [x] `src/lib/auth.ts` — конфиг, callbacks для роли в JWT/session, type-extension в `src/types/next-auth.d.ts`
- [x] `src/proxy.ts` (бывший middleware — Next 16 переименовал) — защита всех приватных роутов, ролевая маршрутизация
- [x] **First-run wizard** `/setup`: server action создаёт первого админа; страница 404 после первого создания
- [x] Главная `/` редиректит на `/setup` если 0 админов; на role-главную если есть сессия
- [x] `/login` с server action `loginAction` через `signIn("credentials")`, обработка ошибок
- [x] Заглушки `/admin`, `/service`, `/client` с приветствием по имени и кнопкой выхода
- [x] Type-check `tsc --noEmit` без ошибок
- [x] Roundtrip-тест через curl: `/` → 307→/setup, `/setup` → 200, после создания админа `/setup` → 404, `/admin` без сессии → 307→/login

**Чекпойнт:** Юзер открывает http://localhost:3000 → редирект на `/setup` → заполняет форму (email + пароль + ФИО) → редирект на `/login` → логинится → попадает на `/admin` и видит «Привет, Иван Админ (admin)». При повторном переходе на `/setup` — 404.

---

## Этап 3. Управление пользователями

- [x] Раздел `/admin/users` — таблица всех пользователей (фильтр по роли — admin/service/client/all)
- [x] Кнопка «Добавить администратора» — форма (имя, email, телефон) → создаётся `User` с ролью `admin`, email-приглашение
- [x] Кнопка «Добавить сервисника» — форма (имя, email, телефон) → создаётся `User` с ролью `service`, отправляется email-приглашение
- [x] Кнопка «Добавить клиента» — форма (ФИО, email, телефон, юр.инфо?) → создаётся `User` (role=client) + `Customer`, отправляется email-приглашение
- [x] Email-инвайт содержит одноразовую ссылку на установку пароля (`/setup-password?token=...`, TTL 7 дней)
- [x] Страница `/setup-password` — форма пароля + подтверждение, после установки → автологин на role-home
- [x] Настройка SMTP в `.env` (Mailpit нативно на Windows — `127.0.0.1:1025` SMTP, UI `127.0.0.1:8025`)
- [x] Возможность деактивировать/активировать пользователя (мягкое удаление: флаг `active=false`)
- [x] Логирование действий в `ActivityLog` (создание/деактивация/активация/повторный инвайт/установка пароля)
- [x] Кнопка «Переотправить инвайт» (если у пользователя ещё нет пароля)

**Чекпойнт:** Админ заводит сервисника и клиента → проверяет Mailpit на http://127.0.0.1:8025 → переходит по ссылке → ставит пароль → заходит как сервисник/клиент.

---

## Этап 4. Карточка клиента + бассейны

- [x] Prisma: `Pool`, `PoolPhoto`, `PoolInstruction`; `individualServicePrice` перенесена с `Customer` на `Pool`. Миграция `20260508121111_pools_init`.
- [x] Раздел `/service/customers` и `/admin/customers` — общий компонент списка с поиском по ФИО/телефону/email.
- [x] Карточка клиента `/[scope]/customers/[id]` — контакты с inline-редактированием и список бассейнов (превью + счётчики).
- [x] Создание/редактирование бассейна — форма `PoolForm` (server actions create/update в `lib/server-actions/pools.ts`).
- [x] Загрузка фото объекта (drag-drop, превью, удаление; soft-limit 10, до 10 МБ; jpg/png/webp/heic).
- [x] Адрес + Яндекс.Карта v3 — компонент `PoolMap` с lazy-load JS API; **бесплатный fallback Leaflet + OpenStreetMap** при отсутствии ключа Я.Карт (ключ нужно вписать в `.env` как `YANDEX_MAPS_API_KEY`). На обеих картах: клик/перетаскивание маркера → координаты в lat/lng.
- [x] Геокодер: кнопка «Найти на карте» рядом с полем адреса → server action в **Nominatim (OSM)**, бесплатно, без ключа. Поля lat/lng скрыты, координаты выставляются автоматически или ручной корректировкой маркера.
- [x] Кнопка «Построить маршрут» открывает `https://yandex.ru/maps/?rtext=~lat,lng&rtt=auto` в новой вкладке.
- [x] Поле «Облицовочные покрытия» (textarea).
- [x] Поле «Дополнительная информация» (textarea).
- [x] Раздел «Инструкции» — добавление PDF / текст / ссылка, удаление; PDF под защищённым роутом.
- [x] Поле «Индивидуальная цена сервиса» на уровне бассейна (без истории).
- [x] Защищённый роут `/api/files/[...path]` с проверкой прав (admin/service — всё; client — только свои бассейны).
- [x] Activity log: `customer.update`, `pool.create`, `pool.update`, `pool.delete`, `pool.photo.upload`, `pool.photo.delete`, `pool.instruction.create`, `pool.instruction.delete`.
- [x] Type-check, lint, `next build` чистые.

**Чекпойнт:** Сервисник (или админ) открывает `/service/customers` → выбирает клиента из этапа 3 → жмёт «+ Бассейн» → создаёт бассейн с именем и адресом → попадает на карточку бассейна → загружает 1-2 фото и текстовую инструкцию → редактирует параметры → кнопка «Построить маршрут» открывает Я.Карты с заданной точкой. Клиент в `/client` пока ничего не видит (его UI — этапы 7+).

---

## Этап 5. Регламент оборудования

- [x] Prisma: `EquipmentTemplate` (тип, гарантия по умолчанию, период регламента) + `Equipment` (привязка к Pool). Миграция `20260508134553_equipment_init`.
- [x] Раздел `/admin/equipment-templates` — CRUD шаблонов (создание, редактирование, скрыть/показать вместо жёсткого удаления, чтобы не терять историю).
- [x] В карточке бассейна — раздел «Оборудование»: добавить из шаблона (с возможностью переопределить гарантию/регламент), серийник, заметки; редактирование, удаление.
- [x] `lib/equipment.ts` — расчёт `warrantyEnd = installDate + warrantyMonths` и `nextRegulation = (lastReplacementDate ?? installDate) + regulationPeriodDays`.
- [x] Кнопка «Заменено сегодня» → обновляет `lastReplacementDate = now`, ActivityLog `equipment.replaced`.
- [x] Виджет на главной сервисника `UpcomingEquipmentWidget`: «Скоро регламент» (≤7 дн) и «Скоро гарантия» (≤14 дн), с переходом на бассейн; виджет скрыт, если событий нет.
- [x] Activity log: `equipmentTemplate.create/update/activate/deactivate`, `equipment.create/update/replaced/delete`.
- [x] Type-check, `next build` чистые.

**Чекпойнт:** Админ открывает `/admin` → «Шаблоны оборудования» → создаёт «Датчик хлора, гарантия 6 мес, регламент 90 дней». Дальше сервисник (или админ) открывает любой бассейн → раздел «Оборудование» → «+ Добавить» → выбирает шаблон → указывает дату установки → видит в карточке даты окончания гарантии и следующего регламента. На главной сервисника появляется виджет «Внимание по оборудованию», если до регламента ≤ 7 дней или до конца гарантии ≤ 14.

---

## Этап 6. Чек-лист (динамическая схема)

- [x] Prisma: `ChecklistQuestion` + enum `ChecklistQuestionType` (text/number/single_select/multi_select/bool). Миграция `20260508151955_checklist_init`.
- [x] Сидер `prisma/seeds/checklist.ts` — 25 вопросов из Google-формы (https://docs.google.com/forms/d/1yjJagwQEUAJULj6aQxYc7707MUDbIV_ev1alBJ-Is9s/viewform), идемпотентный. Скрипт `npm run db:seed:checklist`.
- [x] Раздел `/admin/checklist` — 3 таба (Активные / Скрытые / Превью), drag-drop через `@dnd-kit/sortable`, форма create/edit через URL-параметры (`?new=<type>` / `?edit=<id>`).
- [x] 5 типов вопросов: text, number (с unit), single_select, multi_select, bool. Дубликаты с моделью `Visit` (СОТРУДНИК / ДАТА / НАЗВАНИЕ ОБЪЕКТА / ОТПРАВКА ФОТООТЧЁТА) исключены.
- [x] Превью «как видит сервисник» — переиспользуемый `ChecklistFieldRenderer` (для этапа 8).
- [x] Soft-delete (флаг `active`), без жёсткого удаления, без версионирования вопросов.
- [x] Activity log: `checklist.question.create/update/reorder/activate/deactivate`.
- [x] Type-check, `next build` чистые. Lint — 1 pre-existing ошибка в `AddressAutocomplete.tsx` (этап 4), не блокирующая.

**Чекпойнт:** Админ открывает `/admin/checklist` → видит 25 вопросов из формы → меняет порядок drag-drop'ом → добавляет новый вопрос «Уровень pH запас (число, мг/л)» → видит его в превью.

---

## Этап 7. Календарь и онлайн-запись

- [x] Prisma: `Visit`, `OnlineRequest`, `VisitSeries` + enums `VisitStatus`/`VisitKind`/`OnlineRequestStatus`. Миграция `749b535`.
- [x] Раздел `/service/calendar` — недельный/месячный/дневной/list-вид всех визитов через FullCalendar (MIT-плагины); фильтр по сервиснику; client-side загрузка (`CalendarViewLoader` обёртка вокруг `dynamic({ssr:false})`).
- [x] Создание визита вручную `/service/calendar/new` — общий `<VisitForm />` (выбор клиента → бассейна → даты/длительности → исполнителя), проверка конфликтов через `checkVisitConflicts`.
- [x] Создание серии визитов — чекбокс «Серия повторов» в той же форме: weekly / biweekly / monthly × N повторов, server action `createVisitSeriesAction`.
- [x] Карточка визита `/service/visits/[id]` — просмотр, редактирование (через тот же `<VisitForm />`), отмена с причиной.
- [x] Раздел `/client/request-visit` — выбор бассейна (только свои), желаемый диапазон дат, комментарий; server action `createOnlineRequestAction`.
- [x] Если у клиента есть долг → форма заявки заменяется баннером «Оплатите предыдущий визит» (через `hasUnpaidDebt` — заглушка `false` до этапа 13).
- [x] Раздел `/client/requests` — список своих заявок со статусами (pending/accepted/declined), при accepted — ссылка на визит.
- [x] Главная клиента `/client` — карточки «Записаться на сервис» / «Мои заявки» (с бэйджем pending) и список своих бассейнов.
- [x] Раздел `/service/online-requests` — табы (pending/accepted/declined), кнопка «Принять» → `/service/online-requests/[id]/accept` (тот же `<VisitForm />` с lockedCustomer/lockedPool) → создаётся `Visit`, заявка → `accepted`. Кнопка «Отклонить» (только админ) с причиной.
- [x] Push-уведомления — заглушка `lib/push/stub.ts` через `ActivityLog` + `console.log`: новая заявка → admin/service; принята/отклонена → клиенту.
- [x] Главная сервисника обновлена: карточки Календарь/Онлайн-заявки (бейдж pending), виджет `UpcomingVisitsWidget`.
- [x] Type-check `tsc --noEmit` и `next build` чистые.

**Чекпойнт:** Клиент логинится → видит главную с карточкой «Записаться на сервис» → жмёт → выбирает бассейн, указывает диапазон «12-15 мая», пишет комментарий → отправляет → видит заявку в `/client/requests` со статусом «В обработке». Сервисник логинится → на главной видит бейдж pending → открывает `/service/online-requests` → жмёт «Принять» → выбирает дату 13 мая 10:00, длительность 60 мин → создаётся визит → редирект на `/service/visits/[id]`. Клиент перезагружает `/client/requests` → статус «Принята», дата визита.

---

## Этап 8. Визит, отчёт, PDF

- [ ] Раздел `/service/visit/[id]` — мобильно-адаптивная страница визита
- [ ] Динамический рендер чек-листа из `ChecklistQuestion`
- [ ] Прикрепление фото визита (1+ фото, с компрессией через sharp)
- [ ] Добавление доп.работ (название + сумма, можно несколько)
- [ ] Добавление химии из прайса (выбор + количество, цена фиксируется на момент)
- [ ] Кнопка «Завершить визит» → статус `completed`, генерация PDF
- [ ] PDF: логотип «ХОРОШИЕ БАССЕЙНЫ», ФИО сервисника, дата, ответы чек-листа, фото, таблица доп.работ + химии, итоговая сумма
- [ ] PDF сохраняется в `uploads/reports-pdf/{visit_id}/report.pdf`
- [ ] Клиент видит отчёт в `/client/visits/[id]` (PDF + html-копия)
- [ ] Возможность редактирования отчёта сервисником после завершения (логируется в ActivityLog)

**Чекпойнт:** Сервисник с телефона открывает запланированный визит → заполняет чек-лист → прикрепляет фото → добавляет «Замена картриджа — 1500 ₽» и «Хлор 5 кг» → завершает → клиент в своём ЛК видит PDF.

---

## Этап 9. Прайс химии (админ)

- [ ] Prisma: `ChemistryItem` (имя, единица, цена, active)
- [ ] Раздел `/admin/chemistry` — таблица CRUD
- [ ] Деактивированные позиции не показываются в визите, но остаются в истории
- [ ] Логирование изменения цен в `ActivityLog`

**Чекпойнт:** Админ добавляет «Хлор гранулированный, кг, 800 ₽» → сервисник видит позицию при добавлении химии в визит.

---

## Этап 10. Реестры и журнал

- [ ] Раздел `/admin/registry/checklists` — таблица всех ответов всех визитов: визит / клиент / бассейн / дата / колонки = вопросы
- [ ] Кнопка «Экспорт в Excel» (через `exceljs`) и «Экспорт в CSV»
- [ ] Раздел `/admin/registry/visits` — таблица всех визитов с фильтрами (период, клиент, сервисник)
- [ ] Раздел `/admin/activity-log` — журнал действий с фильтрами (по пользователю, по типу события, по дате)
- [ ] Раздел `/admin/registry/customers` — таблица всех клиентов с долгами/балансом

**Чекпойнт:** Админ открывает реестр чек-листов → видит таблицу ответов → нажимает «Экспорт в Excel» → файл скачивается с корректными данными.

---

## Этап 11. Чат поддержки

- [ ] Prisma: `ChatThread`, `ChatMessage`
- [ ] Раздел `/client/support` — один тред с сообщениями, форма ввода + прикрепление фото
- [ ] Раздел `/service/support` (и `/admin/support`) — список всех тредов, открытие отдельного треда
- [ ] Поллинг каждые 5 секунд при открытом треде (новые сообщения подтягиваются)
- [ ] Photo-вложения сохраняются в `uploads/chat/{thread_id}/`
- [ ] Push-уведомление о новом сообщении (заглушка, реальный push в этапе 12)
- [ ] Индикатор «прочитано»

**Чекпойнт:** Клиент пишет в поддержку «Не работает фильтр» → сервисник видит сообщение через 5 сек → отвечает → клиент видит ответ через 5 сек.

---

## Этап 12. Web Push уведомления

- [ ] Сгенерировать VAPID-ключи, положить в `.env`
- [ ] Service Worker (`public/sw.js`) обрабатывает push-events
- [ ] Подписка пользователя при первом входе (запрос permission)
- [ ] Сохранение подписки в `PushSubscription`
- [ ] Серверная функция `sendPush(userId, title, body, payload)`
- [ ] Подключить ко всем событиям из этапа 7 и 11
- [ ] Уведомление об окончании гарантии (за 14 дней) — пока заглушка-функция, реальный cron в этапе 15
- [ ] Уведомление о регламенте (за 7 дней) — то же

**Чекпойнт:** Юзер ставит CRM на главный экран iPhone (или Android) → разрешает уведомления → тестовая кнопка `/admin/test-push` отправляет себе пуш → пуш приходит даже когда браузер закрыт.

---

## Этап 13. Оплата (полу-онлайн)

- [ ] Prisma: `PaymentStatus` на `Visit` (paid | unpaid | online_pending) + поле `paid_at`
- [ ] В карточке визита сервисник/админ может отметить «Оплачено наличкой/переводом» с датой
- [ ] Расчёт «долга» клиента = сумма всех `unpaid` визитов
- [ ] При попытке клиента создать онлайн-заявку с долгом > 0 → блок с сообщением
- [ ] В админке `/admin/registry/customers` — колонка «Долг»
- [ ] Заготовка `lib/payments/provider.ts` с интерфейсом `PaymentProvider` (метод `createPayment(visit_id, amount)`) + dummy-имплементация. Реальный провайдер подключим позже одной правкой.

**Чекпойнт:** Сервисник отмечает визит как «Оплачено» → у клиента долг 0 → клиент может создать новую онлайн-заявку. Если визит не оплачен → форма заявки заблокирована.

---

## Этап 14. PWA

- [ ] Установить Serwist
- [ ] `manifest.webmanifest` с иконками и названием «Хорошие Бассейны CRM»
- [ ] Иконки 192x192 и 512x512 (логотип компании)
- [ ] Service worker регистрируется автоматически
- [ ] Offline-fallback страница
- [ ] iOS: meta-теги для «Add to Home Screen»
- [ ] Splash-screen для iOS

**Чекпойнт:** Юзер открывает CRM на iPhone в Safari → «Поделиться» → «На экран Домой» → иконка появляется → запуск открывает приложение в полноэкранном режиме без браузерной шапки.

---

## Этап 15. Cron-задачи

- [ ] Worker-процесс `worker/index.ts` запускается отдельно (`npm run worker`)
- [ ] Cron «ежедневно в 09:00»: проверить оборудование с гарантией, истекающей через 14 дней → push клиенту
- [ ] Cron «ежедневно в 09:00»: проверить регламент за 7 дней → push клиенту + всем сервисникам
- [ ] Cron «ежедневно в 03:00»: удалить файлы из `uploads/visit-photos/` и `uploads/chat/` старше 90 дней (физически с диска + записи в БД)
- [ ] Cron «каждые 5 минут»: rescheduled — отправить отложенные пуши из очереди (если делаем очередь)
- [ ] Логирование всех cron-прогонов

**Чекпойнт:** Запускаем `npm run worker` → ставим в шаблоне регламент 8 дней назад → ждём 1 минуту (или меняем расписание на «каждую минуту») → видим лог «Отправлен пуш о регламенте клиенту X» и сам пуш приходит.

---

## Этап 16. Деплой на боевой сервер

- [ ] Dockerfile для Next.js (multi-stage, standalone output)
- [ ] Dockerfile для worker
- [ ] `docker-compose.yml`: postgres + app + worker + nginx
- [ ] `nginx.conf` с reverse-proxy на app, отдача `/uploads/` через защищённый роут (не напрямую)
- [ ] LetsEncrypt через `certbot` или `nginx-certbot` контейнер
- [ ] `.env.production` шаблон
- [ ] Скрипт деплоя `deploy.sh` (git pull + docker compose up -d --build)
- [ ] Скрипт бэкапа `backup-db.sh` + cron на сервере (ежедневно в 02:00, ротация 30 дней)
- [ ] Документация `DEPLOY.md` с инструкциями для системника клиента

**Чекпойнт:** Открываем боевой URL компании в браузере → видим логин-страницу с HTTPS-сертификатом → логинимся как админ → всё работает как локально.

---

## Этап 17. Финальная приёмка

- [ ] Прогон сценария «Админ заводит сервисника и 3 клиентов с бассейнами»
- [ ] Прогон сценария «Клиент отправляет заявку → сервисник принимает → визит → PDF»
- [ ] Прогон сценария «Клиент пишет в чат → ответ сервисника → пуш приходит»
- [ ] Прогон сценария «Регламент через 7 дней → пуши приходят»
- [ ] Прогон сценария «Долг → блокировка записи»
- [ ] Прогон сценария «Экспорт реестра → файл корректный»
- [ ] Чек-лист безопасности: проверка прав доступа к чужим файлам, к чужим визитам, SQL-инъекции (Prisma защищает), XSS в комментариях
- [ ] Передача доступов клиенту, обучение

**Чекпойнт:** Клиент использует CRM в реальной работе минимум неделю без блокирующих багов.

---

## Как продолжить работу в новой сессии

1. Открой `~/good-pools-crm/plan.md` (этот файл).
2. Найди первый незавершённый этап (`[ ]`).
3. Скажи Claude: «Продолжаем CRM с этапа N».
4. Claude прочитает `spec.md` + `plan.md` и поймёт контекст.

**Текущий статус:** этап 6 принят клиентом (2026-05-08). Этап 7 (Календарь и онлайн-запись) реализован, ждёт чекпойнта от юзера. Этапы 8-17 — pending.
