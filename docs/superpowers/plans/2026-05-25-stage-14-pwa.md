# Этап 14. PWA — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Превратить CRM в installable PWA (иконка на домашнем экране, standalone-режим, offline-fallback), не ломая push-уведомления из этапа 12.

**Architecture:** Serwist + кастомный `app/sw.ts` через `@serwist/turbopack` route handler (Next 16 на Turbopack). Один SW: precache + offline catch-handler + перенесённые push handlers. Манифест через built-in `app/manifest.ts`. Offline-страница — статический `public/offline.html`. Иконки — PNG из SVG-лесенки в Header.tsx.

**Tech Stack:** `serwist`, `@serwist/next`, `@serwist/turbopack`, `sharp` (уже стоит), Next.js 16 metadata API.

**Spec:** `docs/superpowers/specs/2026-05-25-stage-14-pwa-design.md`.

**Verification convention в этом проекте:** `tsc --noEmit` + `next build` + Chrome DevTools (Application → Manifest / Service Workers / Lighthouse). Test-фреймворк не используется (см. предыдущие этапы 9-13 в plan.md).

---

## Task 1: Установка зависимостей

**Files:**
- Modify: `package.json` (через `npm install`)

- [ ] **Step 1: Установить Serwist пакеты**

Run:
```
cd C:\Users\Roman\good-pools-crm
npm install serwist @serwist/next @serwist/turbopack
```

Expected: 3 пакета добавлены в `dependencies`, lock-файл обновлён, без ошибок peer-deps.

- [ ] **Step 2: Verify Next 16 compat**

Run:
```
npm ls serwist
```

Expected: версия ≥10.x. Если меньше — это значит npm подтянул старую под Next 15. Принудительно: `npm install serwist@latest @serwist/next@latest @serwist/turbopack@latest`.

- [ ] **Step 3: Commit**

```
git add package.json package-lock.json
git commit -m "этап 14: install Serwist (PWA core + Next adapter + Turbopack route)"
```

---

## Task 2: Скрипт генерации иконок

**Files:**
- Create: `scripts/generate-pwa-icons.ts`
- Delete: `scripts/generate-placeholder-icons.ts`
- Modify: `package.json` (scripts)

- [ ] **Step 1: Создать `scripts/generate-pwa-icons.ts`**

```ts
import sharp from "sharp";
import { mkdir } from "node:fs/promises";
import { dirname } from "node:path";

// SVG: тот же градиент teal-500 → cyan-600 из Header.tsx + белая лесенка-пловец
function buildSvg(size: number, contentScale: number): Buffer {
  const padding = (1 - contentScale) / 2;
  const innerOffset = size * padding;
  const innerSize = size * contentScale;
  const cornerRadius = size * 0.18; // как rounded-xl в Header (~18%)
  return Buffer.from(`<svg xmlns="http://www.w3.org/2000/svg" width="${size}" height="${size}" viewBox="0 0 ${size} ${size}">
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#14b8a6"/>
      <stop offset="100%" stop-color="#0891b2"/>
    </linearGradient>
  </defs>
  <rect width="${size}" height="${size}" rx="${cornerRadius}" fill="url(#g)"/>
  <g transform="translate(${innerOffset},${innerOffset}) scale(${innerSize / 24})" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none">
    <path d="M2 17c2 1 4 1 6 0s4-1 6 0 4 1 6 0"/>
    <path d="M2 21c2 1 4 1 6 0s4-1 6 0 4 1 6 0"/>
    <path d="M6 8a4 4 0 0 1 8 0v8"/>
    <path d="M14 8h4"/>
    <circle cx="14" cy="8" r="1.4" fill="white" stroke="none"/>
  </g>
</svg>`);
}

async function render(svg: Buffer, outPath: string, size: number) {
  await mkdir(dirname(outPath), { recursive: true });
  await sharp(svg).resize(size, size).png().toFile(outPath);
  console.log(`wrote ${outPath}`);
}

(async () => {
  // any-purpose: контент на ~70% поля (как в шапке: иконка внутри rounded-xl с воздухом)
  await render(buildSvg(192, 0.7), "public/icon-192.png", 192);
  await render(buildSvg(512, 0.7), "public/icon-512.png", 512);
  // maskable: safe-zone 80% (контент сжат, чтобы Android adaptive не обрезал)
  await render(buildSvg(512, 0.5), "public/icon-512-maskable.png", 512);
})().catch((err) => { console.error(err); process.exit(1); });
```

- [ ] **Step 2: Удалить старый скрипт-плейсхолдер**

```
git rm scripts/generate-placeholder-icons.ts
```

- [ ] **Step 3: Обновить `package.json` scripts**

В блоке `"scripts"` удалить `"icons:placeholders": "tsx scripts/generate-placeholder-icons.ts"` и добавить `"icons:pwa": "tsx scripts/generate-pwa-icons.ts"`.

- [ ] **Step 4: Запустить скрипт и проверить файлы**

Run:
```
npm run icons:pwa
```

Expected: три строки `wrote public/icon-XXX.png`. После — открыть `public/icon-192.png` глазами: teal-cyan градиент + белая иконка пловца с лесенкой, как в шапке.

- [ ] **Step 5: Commit**

```
git add public/icon-192.png public/icon-512.png public/icon-512-maskable.png scripts/generate-pwa-icons.ts scripts/generate-placeholder-icons.ts package.json
git commit -m "этап 14: PWA icons (192/512/512-maskable) из SVG-лесенки Header.tsx"
```

---

## Task 3: Манифест

**Files:**
- Create: `src/app/manifest.ts`

- [ ] **Step 1: Создать `src/app/manifest.ts`**

```ts
import type { MetadataRoute } from "next";

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: "Хорошие Бассейны — CRM",
    short_name: "ХБ CRM",
    lang: "ru",
    description: "Система обслуживания бассейнов",
    start_url: "/",
    scope: "/",
    display: "standalone",
    orientation: "portrait",
    theme_color: "#0d9488",
    background_color: "#ffffff",
    icons: [
      { src: "/icon-192.png", sizes: "192x192", type: "image/png", purpose: "any" },
      { src: "/icon-512.png", sizes: "512x512", type: "image/png", purpose: "any" },
      { src: "/icon-512-maskable.png", sizes: "512x512", type: "image/png", purpose: "maskable" },
    ],
  };
}
```

- [ ] **Step 2: Проверить генерацию**

Run в фоне:
```
npm run dev
```

Затем в браузере: `http://localhost:3000/manifest.webmanifest` → должен вернуть JSON с полями выше. Остановить dev.

- [ ] **Step 3: Commit**

```
git add src/app/manifest.ts
git commit -m "этап 14: webmanifest через app/manifest.ts (теал-тема, 3 иконки)"
```

---

## Task 4: Offline-страница

**Files:**
- Create: `public/offline.html`

- [ ] **Step 1: Создать `public/offline.html`**

```html
<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>Нет подключения — ХБ CRM</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body { margin: 0; font-family: system-ui, -apple-system, "Segoe UI", sans-serif; background: #fff; color: #111; display: flex; min-height: 100vh; align-items: center; justify-content: center; padding: 24px; }
    .card { max-width: 360px; text-align: center; }
    h1 { font-size: 22px; margin: 0 0 12px; }
    p  { color: #71717a; margin: 0 0 24px; font-size: 15px; }
    button { background: #0d9488; color: #fff; border: 0; border-radius: 10px; padding: 12px 20px; font-size: 15px; font-weight: 500; cursor: pointer; }
    button:hover { background: #0f766e; }
  </style>
</head>
<body>
  <main class="card">
    <h1>Нет подключения</h1>
    <p>Проверьте интернет и обновите страницу.</p>
    <button onclick="location.reload()">Обновить</button>
  </main>
</body>
</html>
```

- [ ] **Step 2: Проверить отдачу**

Run в фоне:
```
npm run dev
```

Затем: `http://localhost:3000/offline.html` → отображается teal-кнопка «Обновить». Остановить dev.

- [ ] **Step 3: Commit**

```
git add public/offline.html
git commit -m "этап 14: статический public/offline.html для offline-fallback"
```

---

## Task 5: Service Worker (Serwist) + перенос push handlers

**Files:**
- Create: `src/app/sw.ts`
- Create: `src/app/sw.js/route.ts`
- Delete: `public/sw.js`
- Modify: `src/proxy.ts` (без изменений, `/sw.js` уже в PUBLIC_PATHS)

- [ ] **Step 1: Создать `src/app/sw.ts`**

```ts
/// <reference lib="webworker" />
/// <reference types="@serwist/next/typings" />

import { Serwist } from "serwist";

declare const self: ServiceWorkerGlobalScope & {
  __SW_MANIFEST: (string | { url: string; revision: string | null })[];
};

const serwist = new Serwist({
  precacheEntries: self.__SW_MANIFEST,
  skipWaiting: true,
  clientsClaim: true,
  navigationPreload: true,
});

serwist.setCatchHandler(async ({ request }) => {
  if (request.destination === "document") {
    const match = await serwist.matchPrecache("/offline.html");
    return match || Response.error();
  }
  return Response.error();
});

serwist.addEventListeners();

// === Push handlers, перенесены 1-в-1 из public/sw.js (этап 12) ===

self.addEventListener("push", (event) => {
  console.log("[SW] push event received, hasData=", !!event.data);
  if (!event.data) {
    console.warn("[SW] no data, abort");
    return;
  }
  let payload: { title: string; body: string; icon?: string; tag?: string; url?: string };
  try {
    payload = event.data.json();
    console.log("[SW] payload=", payload);
  } catch (e) {
    console.error("[SW] JSON parse failed", e);
    return;
  }
  const options: NotificationOptions = {
    body: payload.body,
    icon: payload.icon ?? "/icon-192.png",
    badge: "/badge-72.png",
    tag: payload.tag,
    data: { url: payload.url },
    requireInteraction: false,
  };
  event.waitUntil(
    self.registration
      .showNotification(payload.title, options)
      .then(() => console.log("[SW] showNotification OK"))
      .catch((err) => console.error("[SW] showNotification failed", err)),
  );
});

self.addEventListener("notificationclick", (event) => {
  console.log("[SW] notificationclick");
  event.notification.close();
  const url = (event.notification.data as { url?: string } | undefined)?.url ?? "/";
  event.waitUntil(
    (async () => {
      const all = await self.clients.matchAll({ type: "window", includeUncontrolled: true });
      const target = new URL(url, self.location.origin);
      const existing = all.find((c) => new URL(c.url).pathname === target.pathname);
      if (existing) {
        await (existing as WindowClient).focus();
        return;
      }
      await self.clients.openWindow(url);
    })(),
  );
});
```

- [ ] **Step 2: Создать route handler `src/app/sw.js/route.ts`**

```ts
import { spawnSync } from "node:child_process";
import { createSerwistRoute } from "@serwist/turbopack";

const revision =
  spawnSync("git", ["rev-parse", "HEAD"], { encoding: "utf-8" }).stdout?.trim() ||
  crypto.randomUUID();

export const { dynamic, dynamicParams, revalidate, generateStaticParams, GET } =
  createSerwistRoute({
    swSrc: "src/app/sw.ts",
    additionalPrecacheEntries: [{ url: "/offline.html", revision }],
    useNativeEsbuild: true,
  });
```

- [ ] **Step 3: Удалить старый SW**

```
git rm public/sw.js
```

- [ ] **Step 4: Type-check**

Run:
```
npx tsc --noEmit
```

Expected: 0 errors. Если ругается на `Serwist`, `@serwist/next/typings` или WebWorker lib — это нормально для первой попытки, скорее всего нужен `lib: ["webworker"]` в tsconfig для `sw.ts` или triple-slash references. Если упадёт — поправить inline в `sw.ts` (см. Step 1 — references уже добавлены).

- [ ] **Step 5: Dev-проверка отдачи SW**

Run в фоне:
```
npm run dev
```

В браузере открыть `http://localhost:3000/sw.js` → должен вернуть JS-код service worker'а (большой файл с Serwist + push handlers). Открыть `http://localhost:3000/` → DevTools → Application → Service Workers → должен быть `sw.js` активен. Остановить dev.

- [ ] **Step 6: Commit**

```
git add src/app/sw.ts src/app/sw.js/route.ts public/sw.js
git commit -m "этап 14: Serwist SW (precache + offline catch-handler) + перенос push handlers"
```

---

## Task 6: Подключить манифест и iOS-меты в layout

**Files:**
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Обновить `metadata` в `src/app/layout.tsx`**

Найти текущий `export const metadata: Metadata = { … }` и заменить на:

```ts
export const metadata: Metadata = {
  title: "Хорошие Бассейны — CRM",
  description: "Система обслуживания бассейнов",
  manifest: "/manifest.webmanifest",
  appleWebApp: {
    capable: true,
    statusBarStyle: "default",
    title: "ХБ CRM",
  },
  icons: {
    icon: "/icon-192.png",
    apple: "/icon-192.png",
  },
};

export const viewport: Viewport = {
  themeColor: "#0d9488",
};
```

В шапке файла рядом с `import type { Metadata } from "next";` добавить:

```ts
import type { Metadata, Viewport } from "next";
```

(`themeColor` в Next 16 переехал из `metadata` в отдельный `viewport` export — иначе будет depredecation warning.)

- [ ] **Step 2: Type-check**

Run:
```
npx tsc --noEmit
```

Expected: 0 errors.

- [ ] **Step 3: Commit**

```
git add src/app/layout.tsx
git commit -m "этап 14: подключить manifest + appleWebApp + theme-color в layout"
```

---

## Task 7: Ранняя регистрация SW для всех пользователей

**Files:**
- Create: `src/components/PwaRegister.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Создать `src/components/PwaRegister.tsx`**

```tsx
"use client";

import { useEffect } from "react";

export function PwaRegister() {
  useEffect(() => {
    if (typeof window === "undefined") return;
    if (!("serviceWorker" in navigator)) return;
    navigator.serviceWorker
      .getRegistration("/sw.js")
      .then((reg) => {
        if (!reg) {
          return navigator.serviceWorker.register("/sw.js");
        }
      })
      .catch((err) => {
        console.warn("[PwaRegister] SW register failed", err);
      });
  }, []);
  return null;
}
```

- [ ] **Step 2: Вставить в `src/app/layout.tsx`**

В шапке файла добавить импорт:

```ts
import { PwaRegister } from "@/components/PwaRegister";
```

Внутри `<body>...</body>` добавить `<PwaRegister />` первой строкой:

```tsx
<body className="...">
  <PwaRegister />
  {children}
</body>
```

- [ ] **Step 3: Type-check**

Run:
```
npx tsc --noEmit
```

Expected: 0 errors.

- [ ] **Step 4: Dev-проверка регистрации**

Run в фоне:
```
npm run dev
```

Открыть приватное окно `http://localhost:3000/` → DevTools → Application → Service Workers → виден `sw.js` (active) даже без клика на push-баннер. Cache Storage → `serwist-precache-*` содержит chunks приложения + `/offline.html`. Остановить dev.

- [ ] **Step 5: Commit**

```
git add src/components/PwaRegister.tsx src/app/layout.tsx
git commit -m "этап 14: PwaRegister — ранняя регистрация SW без зависимости от push-баннера"
```

---

## Task 8: Build + offline-проверка

**Files:** (только верификация)

- [ ] **Step 1: Полный билд**

Run:
```
npm run build
```

Expected: build проходит. В выводе видны:
- генерация `/manifest.webmanifest`
- генерация `/sw.js` (Serwist precache manifest injected)
- precache list содержит `/offline.html` + статические ассеты `_next/static/...`

Если падает с ошибкой про `serwist` / Turbopack совместимость — проверить версии (`npm ls serwist`), посмотреть `next.config.*` (может потребоваться `experimental.turbopack` или Serwist-specific конфиг — для Next 16 на Turbopack `createSerwistRoute` должен работать без `next.config` правок, но если ругается — добавить минимально).

- [ ] **Step 2: Запустить prod-сервер**

Run в фоне:
```
npm run start
```

- [ ] **Step 3: Chrome offline-проверка**

В обычном окне: `http://localhost:3000/login` → залогиниться → подождать активации SW (Application → Service Workers → status `activated and running`).

Затем: DevTools → Network → выставить throttling `Offline` → перезагрузить страницу → должна открыться `public/offline.html` с кнопкой «Обновить».

Снять offline-throttling, остановить prod-сервер.

- [ ] **Step 4: Lighthouse PWA audit**

В DevTools → Lighthouse → Mode: Navigation, Categories: PWA → запустить на `http://localhost:3000/login`.

Expected: PWA installable galочка. Если нет — посмотреть warnings в отчёте (обычно: maskable icon, theme-color, viewport meta). Поправить и пересобрать.

- [ ] **Step 5: Manifest check в DevTools**

DevTools → Application → Manifest → проверить: name, short_name, icons (3 шт, превью видны), theme_color teal, display standalone, start_url `/`. Должно быть зелёное «Installable».

- [ ] **Step 6: Commit (если были фиксы)**

Если на шагах 3-5 потребовались правки — commit:
```
git add -A
git commit -m "этап 14: фиксы по результатам PWA-аудита"
```

---

## Task 9: Обновить plan.md и финальный commit

**Files:**
- Modify: `plan.md`

- [ ] **Step 1: Отметить все галочки этапа 14**

В `plan.md` в секции `## Этап 14. PWA` заменить все `- [ ]` на `- [x]`:

```
- [x] Установить Serwist
- [x] `manifest.webmanifest` с иконками и названием «Хорошие Бассейны CRM»
- [x] Иконки 192x192 и 512x512 (логотип компании)
- [x] Service worker регистрируется автоматически
- [x] Offline-fallback страница
- [x] iOS: meta-теги для «Add to Home Screen»
- [x] Splash-screen для iOS
```

**Уточнение по последнему пункту:** написать сразу под Чекпойнтом этапа 14: «*Splash-screen: метаданные через `appleWebApp` дают iOS дефолтный белый splash с иконкой. Полный комплект `apple-touch-startup-image` под 12+ размеров — отложено, добавим если клиент попросит. См. spec §iOS Add-to-Home.*»

- [ ] **Step 2: Обновить блок «Текущий статус» в конце `plan.md`**

Заменить последнюю строку «Этапы 14-17 — pending» на:

```
**Текущий статус (2026-05-25):** этап 8 принят клиентом (2026-05-11). Этапы 9-11 реализованы, ждут чекпойнтов. Этап 12 реализован: код подтверждён end-to-end через диагностические логи; визуальная проверка popup'ов на тестовой машине заблокирована Win-настройками (не код). Этап 13 реализован, ждёт чекпойнта. Этап 14 реализован, ждёт чекпойнта. Этапы 15-17 — pending.
```

- [ ] **Step 3: Commit**

```
git add plan.md
git commit -m "этап 14: реализован, ждёт чекпойнта (mark plan.md)"
```

---

## Чекпойнт для пользователя (после Task 9)

Сообщить юзеру:

> Этап 14 в коде готов. Для приёмки нужно:
>
> **Desktop Chrome:** `http://localhost:3000/login` → DevTools → Application → Manifest должен быть зелёный «Installable». В адресной строке появится иконка «установить приложение» (`+`). Установить → запустится в standalone-окне без браузерной шапки.
>
> **iPhone (Safari):** открыть прод-URL CRM (или прокинуть localhost через ngrok/tailscale) → «Поделиться» → «На экран Домой» → иконка с teal-лесенкой появится → запуск открывает приложение в полноэкранном режиме.
>
> **Offline:** DevTools → Network → Offline → перезагрузка → должна открыться страница «Нет подключения» с кнопкой «Обновить».
>
> Если visual icon выглядит криво (например, в Android adaptive обрезает) — крутим `contentScale` в `scripts/generate-pwa-icons.ts` и перегенерируем.

---

## Self-review (заполнено после написания плана)

**Spec coverage:**
- §Архитектура SW → Task 1 (install) + Task 5 (sw.ts + route handler + delete old). ✓
- §Манифест → Task 3. ✓
- §Иконки → Task 2. ✓
- §Offline-fallback → Task 4 (страница) + Task 5 (catch handler). ✓
- §iOS Add-to-Home + splash → Task 6 (appleWebApp metadata). ✓
- §Регистрация SW → Task 7 (PwaRegister) + Task 6 (layout metadata). ✓
- §Тестирование → Task 8 (build + offline + Lighthouse + Manifest panel). ✓
- §Out of scope (push outbox, splash full set, background sync, update-prompt) — не покрывается, и не должно.

**Placeholder scan:** Нет TBD/TODO/«implement later». Все code blocks полные.

**Type consistency:** `__SW_MANIFEST` тип одинаковый везде. `PwaRegister` экспортируется именованно и так же импортируется. `Viewport` импорт добавлен явно в Task 6. ✓

**Pitfall noted:** В Task 5 Step 4 и Task 8 Step 1 заложена обработка «если Serwist ругается на Turbopack / tsconfig» — это известное место риска первой интеграции с Next 16.
